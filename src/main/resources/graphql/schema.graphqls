type Query {
    bookById(id: ID!): Book
    init: Int
}

type Book {
    id: ID!
    name: String!
    pageCount: Int!
    author: Author
}

type Author {
    firstName: String!
    lastName: String!
    id: ID!
}