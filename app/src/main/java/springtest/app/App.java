/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package springtest.app;

import springtest.list.LinkedList;

import static springtest.utilities.StringUtils.join;
import static springtest.utilities.StringUtils.split;
import static springtest.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
