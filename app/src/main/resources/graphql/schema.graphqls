type Query {
    bookById(id: ID!): Book!
}

type Book {
    id: ID!
    name: String!
    pageCount: Int!
    author: Author!
}

type Author {
    firstName: String!
    lastName: String!
    id: ID!
}